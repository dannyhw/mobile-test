{
  "name": "mobile-test-monorepo",
  "private": true,
  "scripts": {
    "example": "cd packages/example-app && bun start",
    "test:android": "cd packages/example-app && bun test:e2e:android",
    "test:ios": "cd packages/example-app && bun test:e2e",
    "build:all": "cd packages/mobile-test && bun run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.3.10",
  "devDependencies": {
    "@changesets/cli": "^3.0.2"
  }
}
